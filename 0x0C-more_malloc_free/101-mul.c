#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * mul - multiply two positive integers
 * @num1: first integer
 * @num2: second integer
 * @result: result of multiplication
 * Return: new void pointer or NULL if it fails
 */


int mul(int result, int i, int num1, int num2) {
    if (i<b) {
        return Product(result+a,i+1,num1,num2);
    } else {
        return result;
    }
}
