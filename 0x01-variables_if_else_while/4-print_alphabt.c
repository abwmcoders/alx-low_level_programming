#include <stdio.h>
/**
 * main - entry point
 * Return: Return 0
 */
int main(void)
{
	char alphabet = 'a';

	for (alphabet; alphabet <= 'z'; alphabett++)
	{
		putchar(alphabet);
		if (alphabet == 'e' || alphabet == 'q')
		{
			continue;
	}
	putchar('\n');
	return (0);
}
